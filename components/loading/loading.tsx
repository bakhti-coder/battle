import React, { Fragment } from "react";

const Loading = () => {
  return (
    <Fragment>
      <h1 className="text-black-2 text-xl font-semibold">Loading...</h1>
    </Fragment>
  );
};

export default Loading;
